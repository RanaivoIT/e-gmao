{% extends "base.html.twig" %}
{% block title %} {{title}} {% endblock %}

{% block body %}

<div class="content">
    <div class="card  my-4">
        <div class="card-header">
            <span class="card-title h5">Modifer l'intervention</span>
        </div>
        <div class="card-body">

            {{ form_start(form, {'action': path('interventions_edit', {'id':intervention.id}), 'method': 'POST'}) }}

            {{ form_widget(form) }}

            <button class="btn btn-primary" type="submit">Enregistrer</button>

            {{ form_end(form) }}

        </div>
    </div>
</div>
{% endblock %}
