{% extends "base.html.twig" %}
{% block title %} {{title}} {% endblock %}
{% block body %}

<div class="content">
    <div class="card  my-4">
        <div class="card-header">
            <span class="card-title h5">Voir l'organe</span>
        </div>
        <div class="card-body">

            <div class="row d-flex align-items-stretch">
                <div class="col-4 card shadow m-2">
                    
                    <div class="card-body text-center">
                        <img src="{{ '/uploads/pictures/' ~ organe.colection.picture }}" alt="picture" class="rounded img-fluid" style="width: 200px;">
                        <h5 class="my-3">{{ organe.colection.name }}</h5>
                        <p class="text-muted mb-1">{{ organe.colection.maker }}</p>
                        <p class="text-muted mb-1">{{ organe.colection.origin }}</p>
                        
                    </div>
                   
                </div>
                <div class="col card shadow m-2">
                    
                    <div class="card-body">
                        <div class="row responsive">
                            <div class="col-sm-3">
                                <p class="mb-0">Collection </p>
                            </div>
                            <div class="col-sm-9">
                                <p class="text-muted mb-0">{{ organe.colection.name }}</p>
                            </div>
                        </div>
                        <hr>
                        <div class="row responsive">
                            <div class="col-sm-3">
                                <p class="mb-0">Nom </p>
                            </div>
                            <div class="col-sm-9">
                                <p class="text-muted mb-0">{{ organe.name }}</p>
                            </div>
                        </div>
                        <hr>
                        <div class="row">
                            <div class="col-sm-3">
                                <p class="mb-0">Abbreviation</p>
                            </div>
                            <div class="col-sm-9">
                                <p class="text-muted mb-0">{{ organe.abbreviation }}</p>
                            </div>
                        </div>
                        <hr>                  
                        <div class="row">
                            <div class="col-sm-3">
                                <p class="mb-0">Description</p>
                            </div>
                            <div class="col-sm-9">
                                <p class="text-muted mb-0">{{ organe.description }}</p>
                            </div>
                        </div>
                        
                        <hr>
                        <div class="row">
                            <div class="col-sm-3">
                                <p class="mb-0">Pieces</p>
                            </div>
                            <div class="col-sm-9">
                                <p class="text-muted mb-0">{{ organe.pieces | length }}</p>
                            </div>
                        </div>

                        <hr>
                        
                        <div class="d-flex justify-content-start">
                            
                            {% if is_granted('ROLE_ADMINISTRATEUR') %}
                                <a href="{{ path('organes_edit', {'id': organe.id}) }}" type="button" class="btn btn-success me-2">Modifier</a>
                                <a href="{{ path('organes_remove', {'id': organe.id}) }}" class="btn btn-danger me-2">Supprimer</a>
                            {% endif %}
                            <a class="btn btn-primary me-2" href="">Imprimer</a>
                            <a class="btn btn-secondary" href="{{ path('organes') }}">Liste</a>
                        </div>
                        
                    </div>   
                </div>
            </div>

            

        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    <script>
        window.addEventListener('DOMContentLoaded', event => {
            const datatablesSimple = document.getElementById('dataTable');
            if (datatablesSimple) {
                new simpleDatatables.DataTable(datatablesSimple);
            }
        });
    </script>
{% endblock %}